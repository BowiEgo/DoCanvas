import createApp from './createApp'
import { h } from './h'
import { createLayer, createElement } from './canvas'

const XCanvas = {
  createApp,
  h,
  createLayer,
  createElement
}

export default XCanvas
